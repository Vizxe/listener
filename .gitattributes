# Auto detect text files and perform LF normalization
* text=auto

# Batch files are run by cmd.exe, which wants CRLF.
*.cmd text eol=crlf

# Vendored, and not written by this project -- keep it out of diffs and out of
# the language statistics.
viewer/vendor/** linguist-vendored
